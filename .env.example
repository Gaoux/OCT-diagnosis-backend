# ------------------------
# 📦 DATABASE CONFIGURATION
# ------------------------

DB_NAME=oct_sense
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=db
DB_PORT=5432

# Used to initialize the PostgreSQL container
POSTGRES_DB=oct_sense
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_db_password


# ------------------------
# 🌐 FRONTEND & CORS
# ------------------------

FRONTEND_URL=http://localhost:3000

# Allow CORS requests from local frontend dev servers
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ------------------------
# 🔐 DJANGO SECURITY
# ------------------------

SECRET_KEY=django-insecure-please-change-this-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Environment type (development / production)
ENV=development


# ------------------------
# 📧 EMAIL (SMTP Configuration)
# Use your desired SMTP provider (Mailtrap, Gmail, SendGrid, etc.)
# ------------------------

EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_HOST_USER=your_smtp_user
EMAIL_HOST_PASSWORD=your_smtp_password
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=noreply@octsense.com


# ------------------------
# 👤 ADMIN USER (Created Automatically)
# ------------------------

ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123
